function reverse_string(str) {
  let reversed = '';
  for (let i = str.length - 1; i >= 0; i--) {
    reversed += str[i];
  }
  return reversed;
}

function reversed(str) {
  let revere = '';
  for (let i = str.length - 1; i >= 0; i--) {
    revere += str[i];
  }

  return revere;
}
